apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  namespace: enerlink
spec:
  selector:
    matchLabels:
      app: jenkins-demo
  template:
    metadata:
      labels:
        app: jenkins-demo
    spec:
      containers:
        - image: ccr.ccs.tencentyun.com/enerlink/demo:d7595d3.76
          imagePullPolicy: IfNotPresent
          name: jenkins-demo
